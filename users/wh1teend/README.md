# wh1teend userspace

Documentation lives in [`docs/`](../../docs) — a Fumadocs site (EN + RU):

```sh
cd docs && pnpm dev   # http://localhost:3000
```

Pages: overview & build, keys & trackball modes, configuration reference.
